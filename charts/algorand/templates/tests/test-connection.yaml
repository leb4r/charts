apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "algorand.fullname" . }}-test-connection"
  labels:
    {{- include "algorand.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "algorand.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
